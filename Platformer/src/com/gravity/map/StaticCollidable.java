package com.gravity.map;import java.util.Collection;import org.newdawn.slick.geom.Vector2f;import com.gravity.geom.Rect;import com.gravity.physics.Collidable;import com.gravity.physics.RectCollision;/** * Represents a convex shape in map terrain for collision detection. *  * Also provides some default method implementations assuming that the Tile is constant *  * @author xiao */public class StaticCollidable implements Collidable {    private Rect shape;    public StaticCollidable(Rect shape) {        this.shape = shape;    }    @Override    public void handleCollisions(float ticks, Collection<RectCollision> collisions) {        // No-op    }    @Override    public void rehandleCollisions(float ticks, Collection<RectCollision> collisions) {        // No-op    }    @Override    public Vector2f getPosition(float millis) {        return shape.getPosition();    }    @Override    public Rect getRect(float millis) {        return shape;    }    @Override    public String toString() {        return "StaticCollidable [shape=" + shape + "]";    }    @Override    public boolean causesCollisionsWith(Collidable other) {        return true;    }}